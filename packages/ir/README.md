# @asha-rpg/ir

Immutable TypeScript types for normalized `asha.rpg.ir@1` plus checked operation
and capability versions generated from the Rust semantic registry.

This package contains no evaluator and has no runtime dependencies.
