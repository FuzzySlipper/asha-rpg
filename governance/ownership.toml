# Source and owner-local tests activate a cell together.

[crate."crates/rpg-core"]
lane = "rust-domain"
implementation_status = "active"
may_depend_on = ["serde"]
owns = ["authority values", "open team identities", "typed intent cell and area-selection bindings", "contextual contribution contracts and accepted DomainEvents", "activation-budget capability state and owner", "stable portable state construction"]

[crate."crates/rpg-ir"]
lane = "rust-language"
implementation_status = "active"
may_depend_on = ["rpg-core", "serde"]
owns = ["normalized RPG IR and bounded target selectors", "Ruleset wire contract", "compiled PlayBundle artifact wire contract", "typed derivation and overlay provenance wire contracts"]

[crate."crates/rpg-compiler"]
lane = "rust-language"
implementation_status = "active"
may_depend_on = ["rpg-core", "rpg-ir", "serde", "serde_json"]
owns = ["semantic compilation", "private compiled plans", "Ruleset provision validation", "activation timing, affordability, payment, and trace", "contextual contribution evaluation and trace", "PlayBundle artifact validation and fingerprints", "materialized Content Pack definition validation"]

[crate."crates/rpg-runtime"]
lane = "rust-authority"
implementation_status = "active"
may_depend_on = ["rpg-core", "rpg-ir", "rpg-compiler", "serde", "serde_json"]
owns = ["PlayBundle-bound Scenario validation", "single authority session", "turn, activation-budget, and encounter readback authority", "legal participant, cell-path, and bounded-area option projection", "activation-budget reset transitions", "encounter-owned contribution context facts", "host-supplied random evidence interface", "bounded roll-tape source", "portable checkpoint and replay contracts", "canonical session state hash", "pending reaction restore"]

[crate."crates/asha-rpg"]
lane = "rust-api"
implementation_status = "active"
may_depend_on = ["rpg-core", "rpg-ir", "rpg-compiler", "rpg-runtime"]

[package."packages/ir"]
name = "@asha-rpg/ir"
lane = "ts-authoring"
type = "lib"
layer = "protocol"
implementation_status = "active"
may_depend_on = []

[package."packages/authoring"]
name = "@asha-rpg/authoring"
lane = "ts-authoring"
type = "lib"
layer = "domain"
implementation_status = "active"
may_depend_on = ["@asha-rpg/ir"]
owns = ["immutable Ruleset, Content Pack, PlayBundle, Scenario, bounded-area selector, and scalar contribution authoring contracts", "explicit Content Pack dependency resolution", "deterministic base, mixin, overlay, and configuration materialization into prepared PlayBundles"]
